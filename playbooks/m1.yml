- hosts: M1-Nando.local
  connection: local
  vars_files:
    - "{{ playbook_dir }}/vars/main.yml"
  tasks:
    - name: Install everything for M1
      ansible.builtin.include_tasks:
        file: "{{ playbook_dir }}/tasks/everything.yml"
