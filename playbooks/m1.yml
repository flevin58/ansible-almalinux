- hosts: M1-Nando.local
  connection: local
  vars_files:
    - vars/main.yml
  tasks:
    - name: Install everything for M1
      ansible.builtin.include_tasks:
        file: ../tasks/everything.yml
